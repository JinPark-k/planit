/**
 * 간격 스케일. 4의 배수가 기본이고, 머리카락 두께 조정에만 xxs(2)를 쓴다.
 *
 * 값을 직접 쓰지 않는 이유: 역할이 같은데 값이 갈리는 일이 실제로 있었다.
 * 키워드 칩(상하 9) / 일차 탭(7) / 태그(5) / 카테고리 배지(4)는 전부 같은
 * 알약 모양인데 네 군데가 다 달랐고, 뒤 둘은 같은 파일 안에서도 갈렸다.
 * 고를 수 있는 값을 좁혀 두면 눈으로 맞출 일이 없어진다.
 */
export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
} as const;
