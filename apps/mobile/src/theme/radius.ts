/**
 * 모서리 반경.
 *
 * pill이 999인 이유: 칩·탭처럼 높이가 내용에 따라 변하는 요소에 고정값(16, 18…)을
 * 주면 높이가 바뀔 때 완전한 알약이 되지 않고, 화면마다 다른 값을 골라 쓰게 된다.
 * 높이보다 큰 값을 주면 항상 정확한 반원이라 고민할 여지가 사라진다.
 */
export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  pill: 999,
} as const;
