<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      상태바 칩/NowBar 승격(Android 16 Live Updates)에 필요하다. 런타임 권한이
      아니라 install-time이고, API 36 미만 기기에서는 무시된다.
    -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
      여행이 도는 동안 포그라운드 서비스로 알림을 유지한다. 갤럭시가 앱별
      "실시간 정보" 자격을 판단하는 기준으로 보이고, 플랫폼 가이드도 오래 도는
      live 경험은 타입을 선언한 FGS 위에서 돌라고 말한다. 카메라·위치·미디어 등
      기존 타입 중 맞는 게 없어 specialUse를 쓴다.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!--
        알람 발화 / 부팅 완료 / 시간·시간대 변경 시 저장된 여행 플랜으로
        render(now)를 다시 돌리고 다음 알람을 재예약한다(TripAlarmReceiver
        참고). exported="false"이지만 BOOT_COMPLETED/TIME_SET/TIMEZONE_CHANGED는
        시스템만 보낼 수 있는 protected broadcast라 정상 수신된다 — 알람 발화와
        "여행 종료" 액션은 명시적 Intent로 직접 보내므로 별도 필터가 필요 없다.
      -->
      <!--
        삼성 전용 후크. One UI의 Ongoing Activity(NowBar/실시간 정보)가 앱을
        후보로 인식할 때 보는 값으로 알려져 있다. AOSP 표준만으로는 갤럭시에서
        canPostPromotedNotifications()가 계속 false였고 앱별 "실시간 정보" 설정
        자체가 나타나지 않았다. 삼성이 문서화하지 않은 영역이라 확실하지 않지만,
        다른 기기에서는 모르는 meta-data로 무시되므로 넣어 두는 비용이 없다.
      -->
      <meta-data
        android:name="com.samsung.android.support.ongoing_activity"
        android:value="true" />

      <service
        android:name=".liveupdate.LiveTripService"
        android:exported="false"
        android:foregroundServiceType="specialUse">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="여행 일정 진행 상황을 잠금화면·상태바에 실시간으로 표시" />
      </service>

      <receiver
        android:name=".liveupdate.TripAlarmReceiver"
        android:exported="false">
        <intent-filter>
            <action android:name="android.intent.action.BOOT_COMPLETED" />
            <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            <action android:name="android.intent.action.TIME_SET" />
            <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        </intent-filter>
      </receiver>
    </application>
</manifest>
