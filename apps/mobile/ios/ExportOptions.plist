<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- App Store Connect(TestFlight 포함)로 올리는 빌드다. -->
	<key>method</key>
	<string>app-store-connect</string>

	<key>teamID</key>
	<string>Y4Q4TS7RTR</string>

	<!-- 프로젝트 Release가 수동 서명이라 익스포트도 맞춘다. 자동으로 두면
	     xcodebuild가 프로파일을 스스로 찾으려다 실패한다
	     ("No profiles for 'com.mjj.planit' were found"). -->
	<key>signingStyle</key>
	<string>manual</string>

	<key>signingCertificate</key>
	<string>iPhone Distribution</string>

	<!-- 위젯 익스텐션(PlanitLiveActivity)도 별도 번들 ID/프로파일이 필요하다.
	     여기 빠뜨리면 xcodebuild -exportArchive가
	     "No profiles for 'com.mjj.planit.LiveActivity' were found"로 실패한다
	     — 위 "planit AppStore" 항목이 예전에 같은 이유로 겪었던 사고와 동일한
	     패턴이니 이후 타겟을 추가할 때도 잊지 말 것. -->
	<key>provisioningProfiles</key>
	<dict>
		<key>com.mjj.planit</key>
		<string>planit AppStore</string>
		<key>com.mjj.planit.LiveActivity</key>
		<string>planit LiveActivity AppStore</string>
	</dict>

	<!-- 심볼을 함께 올려야 App Store Connect에서 크래시 로그가 해석된다. -->
	<key>uploadSymbols</key>
	<true/>

	<!-- 앱 씬 크기를 줄이는 최적화. App Store 배포의 기본값이다. -->
	<key>stripSwiftSymbols</key>
	<true/>
</dict>
</plist>
